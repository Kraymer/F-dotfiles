{
    // run flake8 lint on file loading
    "lint_on_load": true,

    // run lint in live mode: lint file (without popup) every XXX ms
    "live_mode": true,

    // skip errors and warnings (e.g. ["E303", "E4", "W"])
    "ignore": ["D", "E126", "E121", "E123", "E128", "N806", "W503"],

    // complexity check (set number > 0 to check complexity level)
    "complexity": 12,

    "pep8_max_line_length": 99,
}
